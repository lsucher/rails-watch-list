<h1><%= @list.name %></h1>

<p><%= link_to 'Update list', edit_list_path %></p>
<p><%= link_to "Delete #{@list.name}", lists_path(@list), method: :delete, data: { confirm: "Are you sure?" }%></p>
<p><%= link_to 'Back to overview', lists_path %></p>
